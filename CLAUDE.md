# 강의노트 프로젝트

권규상 교수의 학기별 강의노트를 **HTML 슬라이드**로 만들고, 웹으로 배포하며, 페이지에서 **슬라이드 형식 PDF**를 내려받게 하는 프로젝트다.

기존에는 PowerPoint로 만들어 PDF로 출력해 강의했다. 앞으로는 HTML이 원본이고 PDF는 그 산출물이다.

---

## 반드시 먼저 지킬 것

### 1. 공개/비공개 경계

학기 폴더에는 **시험지, 성적, 채점표, 문제은행**이 함께 들어 있다. 웹에 올라가면 사고다.

| 폴더 | 공개 | 내용 |
|---|---|---|
| `slides/` | 공개 | 강의노트 HTML |
| `pdf/` | 공개 | 빌드된 PDF |
| `assets/` | 공개 | 강의용 이미지 |
| `source/` | **비공개** | 원본 pptx·pdf, 교재 스캔, OCR(`_ocr.txt`), 판독물(`_dossier.md`) |
| `private/` | **비공개** | 시험지(hwp), 성적·채점(xlsx), 문제은행 |

- 배포 산출물에는 `slides/`, `pdf/`, `assets/`, `index.html`, `_shared/`만 담는다. 화이트리스트 방식이며, 제외 목록으로 관리하지 않는다.
- **`private/`의 시험지·성적·문제은행은 슬라이드에 인용하거나 요약해 넣지 않는다.** 시험 문제가 강의노트로 새어 나가는 경로다.
- `source/`의 교재 원본과 판독물(`_dossier.md`)은 성격이 다르다 — 강의노트를 만드는 **입력**이므로 슬라이드 작성에 쓴다. 다만 **원문을 그대로 옮겨 배포하지는 않는다** (교재 판권면에 "무단전재와 복제를 금합니다"). 슬라이드에는 소화한 개념과 재작도한 도식만 들어간다.
- 새 파일을 만들 때 이 파일이 어느 칸에 속하는지 먼저 정한다.

### 2. Google Drive 동기화 폴더다

작업 폴더가 Google Drive 동기화 대상이다. 파일 수가 폭증하면 동기화가 망가진다.

- **`npm install`을 이 폴더에서 실행하지 않는다.** `node_modules`를 만들지 않는다.
- 라이브러리는 필요한 dist 파일만 `_shared/vendor/`에 직접 둔다. 현재 `_shared/` 전체가 18개 파일이다 (절반이 폰트·vendor). 이 규모를 유지한다.
- 일회성 도구는 `npx`로 실행한다. 캐시가 `~/.npm`(Drive 밖)에 남는다.
- 임시 파일은 이 폴더가 아니라 시스템 임시 디렉터리에 만든다.

**셸에서 한글 파일명을 glob으로 매칭하지 않는다.** macOS는 파일명을 NFD(자모 분리)로 저장하는데 스크립트에 적는 한글 리터럴은 NFC라서 `for d in *학기_*/` 같은 패턴이 아무것도 못 잡고 조용히 넘어간다. 실제로 `build-site.sh`가 이 문제로 학기 폴더를 통째로 빠뜨렸다. 폴더를 찾을 때는 이름 대신 **`course.json`이 있는지**로 판별한다.

URL에 쓸 때도 마찬가지로 한글 경로는 `urllib.parse.quote`로 인코딩한다 (`build-pdf.sh` 참조).

### 3. 슬라이드를 만들거나 고치기 전에 `design.md`를 읽는다

색·치수·슬라이드 유형이 전부 거기 있다.

**2026-08 에 전면 개편했다.** 이전 판은 기존 PPT 실측값을 규범으로 삼았지만
1~9강이 모두 HTML 로 옮겨진 뒤 맞춰야 할 PPT 가 없어졌다. 지금 성격은
"학술 저널의 편집 지면"이다 — 크림슨 푸터바 없음, 제목은 명조, 표는 괘선 최소,
메인색 `#A02948`.

테마 파일은 `tokens.css` → `cbnu.css` → `components.css` → `print.css` 넷뿐이고
읽는 순서가 곧 덮어쓰는 순서다. **테마 변종은 없다** — 개편기에 쓰던 `jimyeon.css` 와
`data-theme="jm"` 은 2026-08-14 에 이 넷으로 흡수하고 지웠다.

테마 파일을 고치거나 규칙을 옮길 때는 **CSS 가중치부터 확인한다.** 흡수 때
표 정렬·괘선이 조용히 뒤집혔다. 왜 그런지와 두 층으로 나눠 적는 법은 `design.md` 부록에 있다.
바꾼 뒤에는 전 슬라이드의 계산스타일을 헤드리스로 떠서 전후를 대조한다 —
`check-figures.sh` 는 색·활자 회귀를 잡지 못한다.

`design.md`가 글로 적은 규범을 **눈으로 보는 판**이 `_design/`의 미리보기 카드다. claude.ai/design의 **"강의노트 디자인 시스템 (충북대)"** 프로젝트에 올라가 있다.

- 카드는 `_shared/theme/*.css`를 그대로 참조한다. **테마를 고치면 카탈로그도 따라온다** — 색·치수를 두 곳에 적지 않는다.
- `_scripts/stage-design.sh`로 번들을 만들고 미리보기한다. 올리는 건 `DesignSync` 도구로 Claude가 한다.
- 슬라이드 유형을 새로 만들면 `design.md` §5에 항목을 추가하고 `_design/slides/`에 카드도 함께 만든다.

---

## 폴더 구조

```
200_education/
├── CLAUDE.md              이 파일
├── design.md              디자인 시스템 — 슬라이드 작업 전 필독
├── index.html             전체 학기 허브
├── _shared/               전 학기 공용 자산
│   ├── theme/             tokens.css · cbnu.css · components.css · print.css
│   ├── js/                deck-init.js · charts.js
│   ├── fonts/             Pretendard woff2 3종 · BookkMyungjo (제목 명조)
│   ├── img/               cbnu-logo.jpg
│   └── vendor/            reveal · chartjs · mathjax-tex-svg.js
├── _templates/            lecture.html · dossier.md (교재 판독물 규격)
├── _scripts/              serve.sh · build-pdf.sh · build-site.sh · ocr-scan.sh · dossier-read.sh
├── 2026-1학기_도시경제학입문/    (pptx 원본 → 변환 완료)
│   ├── course.json        과목 메타 — 푸터 문구·강의 목록의 원천
│   ├── index.html         이 과목의 강의 목록
│   ├── slides/            02-수요와공급.html …
│   ├── pdf/               빌드 산출물
│   ├── assets/            과목 전용 이미지
│   ├── source/            비공개 — 원본 pptx/pdf
│   └── private/           비공개 — 시험·성적
└── 2026-2학기_도시및지역경제학/  (교재 스캔 원본 — source/N장/ 폴더 하나 = 하루 강의)
```

새 학기를 열 때: `course.json` 만 만들면 빌드·검사 스크립트가 자동 인식한다.
수동 등록은 **루트 `index.html` 의 `COURSES` 배열** 한 곳뿐이다.
과목 `index.html` 은 기존 것을 그대로 복사하면 된다 (과목명이 하드코딩돼 있지 않다).

### 명명 규칙

- 학기 폴더: `YYYY-N학기_과목명` (예: `2026-1학기_도시경제학입문`)
- 슬라이드 파일: `NN-핵심어.html` (예: `02-수요와공급.html`) — 두 자리 강 번호, 공백 없이
- PDF: 슬라이드와 같은 이름 (`02-수요와공급.pdf`)

### `course.json`

과목 메타는 여기 한 곳에만 둔다. 푸터 문구·페이지 번호·다운로드 버튼·강의 목록·**표지 날짜**가 전부 이 파일에서 나온다. 슬라이드 HTML에 과목명·학기·날짜를 하드코딩하지 않는다.

표지 날짜는 `<p class="cover-date" data-course-date>` 로 비워 두면 `deck-init.js` 가 파일명 앞 두 자리로 `lectures` 에서 자기 강의를 찾아 채운다.

**날짜의 기준은 1강 강의계획서(주별 강의 내용)다.** 원본 pptx 표지 날짜는 5~9강에서 강의계획서와 어긋나 있었다.

---

## 강의노트 작성 워크플로

입력은 보통 **교재 내용과 개요를 대화로 전달**받는 형태다. 순서를 지킨다.

**1) 구성안을 먼저 제시하고 확인받는다.**
슬라이드를 바로 쓰지 않는다. 아래 형식으로 목록을 만들어 확인을 받는다.

```
1. [T1 표지] 3강. 소비자 이론과 생산자 이론
2. [T3 정의] 효용 — 소비에서 얻는 만족의 크기
3. [T4 2단] 무차별곡선 — 좌: 정의·성질 4가지 / 우: 곡선 그래프 (3단계 누적)
4. [T7 수식] 한계대체율 — MRS = MUx/MUy 유도
```

유형 표기(`T1`~`T9`)는 `design.md` §5의 카탈로그를 따른다.

**2) `_templates/lecture.html`을 복사해 시작한다.** 빈 파일에서 쓰지 않는다.

**3) 슬라이드를 쓴다.**
- 개념 그래프는 `_shared/js/charts.js`의 함수를 먼저 찾아 쓴다. 없으면 새로 만들되 재사용 가능하게 `charts.js`에 추가한다.
- 실측 데이터 그래프만 Chart.js를 쓴다.
- 누적 공개는 `.fragment`로 한다. 슬라이드를 복제하지 않는다.

**4) 미리보기로 확인한다.** `_scripts/serve.sh`

**5) 그래프를 검사한다.** `_scripts/check-figures.sh` — 좌표점이 곡선을 벗어났는지, 글자가 캔버스 밖으로 잘렸는지, **표·그림이 푸터를 넘었는지** 기계로 잡아낸다. **문제가 0건이 될 때까지 PDF를 뽑지 않는다.**

푸터를 넘으면 순서대로 시도한다: 그림 상한(이미 걸려 있다) → `<div class="body tight">` → `tighter` → 그림·표 자체를 줄인다.

곡선 색 충돌(뜻이 다른 곡선이 같은 색)은 2026-08 부터 검사기가 함께 잡는다.
검사기가 여전히 못 잡는 것은 **라벨끼리의 겹침과 경제 관계의 정합성**이다 —
곡선이 서너 개 들어가는 그림은 만든 뒤 눈으로 확인한다.

**6) PDF를 빌드한다.** `_scripts/build-pdf.sh`

### 내용에 관한 규칙

- **강의 내용을 임의로 창작하지 않는다.** 사용자가 준 교재·개요에 없는 사실, 수치, 사례를 지어내지 않는다. 원본 그림을 다른 것으로 바꾸는 것도 창작이다 — 저작권이 걸리는 이미지라도 원본을 그대로 옮기고 배포 시점에 따로 판단한다.
- **경제 그림은 좌표를 손으로 찍지 않고 관계식에서 유도한다.** MC·AC·AVC 는 비용함수 하나에서, 한계수입은 수요곡선에서, 교차점은 `Fig.cross` 로 구한다. 손으로 찍으면 "MC 가 AC 최저점을 지난다", "MR=MC 인 수량에서 수요가 LAC 에 접한다" 같은 관계가 조용히 깨진다.
- 근거가 불명확하거나 여러 해석이 가능하면 슬라이드에 채워 넣지 말고 **물어본다.**
- 경제학 용어의 한글/영문 병기는 기존 노트 방식을 따른다: `한계편익(marginal benefit)`.
- 사용자가 쓴 표현이 있으면 그대로 쓴다. 매끄럽게 다듬는다며 바꾸지 않는다.

---

## 기존 PPT 변환 워크플로

1학기 1~9강 변환은 완료됐다. 앞으로 pptx 원본 과목이 또 생기면 이 순서를 따른다.

1. `source/`의 pdf를 읽어 슬라이드 순서와 내용을 파악한다. pptx를 풀면 정확한 좌표·색을 잴 수 있다.
2. **누적 공개로 복제된 슬라이드를 하나로 합친다.** 원본에서 같은 제목이 연속 3~4장 나오고 요소만 늘어나면, 그것은 한 슬라이드의 fragment 단계다. 2강 p.2~5가 그 예다.
3. 그래프는 이미지로 캡처하지 말고 SVG로 다시 그린다. 확대해도 깨지지 않고 색을 토큰으로 관리할 수 있다.
4. 변환 후 원본 PDF와 나란히 놓고 대조한다. 누락된 슬라이드가 없는지, 문구가 바뀌지 않았는지 본다.

---

## 교재 스캔 변환 워크플로 (2026-2학기 도시및지역경제학)

입력은 `source/N장/` 의 교재 스캔 jpg다. 장 폴더 하나 = 하루 강의.

1. **판독은 서브에이전트에 맡긴다** (사용자 승인, 2026-08-13). 장마다:
   `_scripts/ocr-scan.sh` → `_ocr.txt` 로 본문 판독 → 그림·표 있는 쪽만 이미지로 열람
   → `source/N장/_dossier.md` 저장 후 종료. 이미지를 본 세션 컨텍스트에 남기지 않는다
   (쪽당 3.5k → 1.1k 토큰).
2. **판독물은 반드시 파일로 남긴다** (2026-08-15). 예전에는 대화로만 넘기고 버려서
   아홉 장 분량이 통째로 사라졌다. 규격은 `_templates/dossier.md`, 실물 예시는 9장.
   `_ocr.txt` 도 지우지 않는다 — 판독물을 다시 만들 때의 원재료다.
   읽기는 `_scripts/dossier-read.sh` (왼쪽 스캔 원본 · 오른쪽 판독 텍스트, 로컬 전용).
3. dossier 규칙: 원문 그대로 옮김 · 전 항목 쪽수 표기 · 판독 불가는 `[판독불가]` ·
   그림은 캡션·축·인쇄된 숫자만 (해석 금지) · **OCR 오독은 스캔을 눈으로 확인한 경우에만
   고치고 고친 자리를 앞머리에 적는다.** 문맥으로 짐작해 고치는 건 창작이다.
   읽기 화면이 스캔을 옆에 붙여 주므로 사소한 OCR 잡음은 남겨 둬도 된다 — 그 자리에서 확인된다.
4. 도식은 SVG 재작도가 기본. **표와 그림이 같은 숫자에서 나오게 상수를 `<head>` 의 JS 로
   한 번만 선언**하고 표 HTML 과 도형 좌표를 거기서 유도한다 (design.md §6.5).
   3D 렌더링처럼 재작도가 불가능한 것만 스캔을 잘라 `assets/tb-*.jpg` 로 둔다 —
   `tb-` 접두사는 교재 유래 표시로, **대외 배포 전 저작권 판단 대상**이다
   (판권면에 "무단전재와 복제를 금합니다". 배포 직전 `assets/tb-*` 로 전수 확인).
5. 판독 안 되는 쪽은 슬라이드를 `.pending` 상자로 비워 두고 `private/스캔-재촬영-필요.md` 에
   기록한다. 확인·결정 대기 항목은 `private/확인-필요-사항.md` 에 모은다.
6. 스캔본을 PDF 로 묶어 받아도 절감이 없다 (쪽마다 이미지 렌더링). 텍스트 레이어가 있는
   PDF 만 `pdftotext` 로 뽑는 게 낫다.

---

## 명령어

```bash
# 디자인 카탈로그 미리보기 (http://localhost:8090)
_scripts/stage-design.sh

# 로컬 미리보기 (http://localhost:8080)
_scripts/serve.sh

# 그래프 검사 — 점이 곡선을 벗어났는지, 글자가 잘렸는지
_scripts/check-figures.sh

# 교재 스캔 폴더 OCR → 폴더 안 _ocr.txt (macOS Vision, 한국어, 로컬 처리)
_scripts/ocr-scan.sh "2026-2학기_도시및지역경제학/source/11장"

# 교재 판독물 읽기 (http://localhost:8091) — 왼쪽 스캔 원본 · 오른쪽 판독 텍스트
# 번들은 Drive 밖에 만든다. 배포하지 않는다 (교재 저작권).
_scripts/dossier-read.sh

# PDF 빌드 — 인자 없으면 전체, 있으면 해당 파일만
_scripts/build-pdf.sh
_scripts/build-pdf.sh 2026-1학기_도시경제학입문/slides/02-수요와공급.html

# 배포용 정적 사이트 생성 (화이트리스트 복사 → dist/)
_scripts/build-site.sh
```

PDF는 `npx decktape`가 헤드리스 Chrome으로 만든다. 브라우저 인쇄 대화상자에 의존하지 않는다 — 사용자 환경마다 결과가 달라지기 때문이다.

---

## 배포

아직 설정하지 않았다. `_scripts/build-site.sh`가 `dist/`를 만드는 데까지만 되어 있다. GitHub Pages로 올리기로 하면 그때 저장소와 워크플로를 붙인다.

Drive 폴더 안에서 `git init`을 하면 `.git`이 동기화 충돌을 일으킬 수 있다. 배포를 실제로 진행할 때 별도로 판단한다. **먼저 물어보지 않고 `git init`을 실행하지 않는다.**
