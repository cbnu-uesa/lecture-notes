# 강의노트 디자인 시스템

이 문서는 HTML 슬라이드 강의노트의 시각 규범이다. **슬라이드를 만들거나 고치기 전에 반드시 읽는다.**

여기 있는 수치는 임의로 정한 값이 아니다. 기존 PowerPoint 강의노트(`2026-1학기_도시경제학입문/source/`)의 원본 XML을 해체해 실측한 값이다. 근거가 되는 슬라이드를 함께 적어 두었으므로, 값을 바꾸려면 그 슬라이드를 먼저 확인한다.

---

## 1. 원칙

1. **한 슬라이드에 한 메시지.** 기존 노트의 슬라이드당 평균 텍스트는 3~5줄이다. 넘어가면 슬라이드를 쪼갠다.
2. **여백이 기본값이다.** 기존 노트는 화면의 절반 이상이 비어 있는 슬라이드가 많다. 빈 곳을 채우려 들지 않는다.
3. **색은 의미로만 쓴다.** 장식용 색은 없다. 아래 §3의 역할표에 없는 용도로 색을 쓰지 않는다.
4. **그래프는 개념도다.** 축의 눈금은 설명에 필요한 점만 찍는다. 격자·범례·눈금 전체 표시는 하지 않는다.
5. **애니메이션은 누적 공개뿐이다.** 전환 효과, 페이드, 슬라이드 인은 쓰지 않는다.

---

## 2. 좌표계와 서체

### 2.1 슬라이드 크기

원본 PPT는 `12192000 × 6858000` EMU = **960 × 540 pt** (16:9)다.

Reveal.js를 `width: 960, height: 540`으로 설정하면 **PPT의 pt 값과 CSS px가 정확히 1:1로 대응한다.** 그래서 이 문서의 모든 치수는 px 단위이며, 원본에서 잰 pt 값을 그대로 옮긴 것이다. 화면 크기 대응은 Reveal이 전체를 배율 조정해 처리하므로, 어떤 요소도 `%`나 `vw`로 잡지 않는다.

```js
Reveal.initialize({ width: 960, height: 540, margin: 0, minScale: 0.2, maxScale: 2.0 })
```

### 2.2 서체

| 원본 (PPT) | 사용처 | HTML 대응 |
|---|---|---|
| KoPub돋움체 Bold | 제목, 강조 | Pretendard **700** |
| KoPub돋움체 Medium | 본문 대부분 (1783회) | Pretendard **500** |
| KoPub돋움체 Light | 부연, 캡션 (514회) | Pretendard **400** |
| Calibri | 영문·숫자 | Pretendard가 함께 처리 |
| Arial | 푸터바 | Pretendard **400** |
| Cambria Math | 수식 | MathJax 기본 수학 서체 |

`_shared/fonts/`에 Pretendard Regular·Medium·Bold woff2 3종을 두고 `@font-face`로 직접 로드한다. CDN을 쓰지 않는다 — 오프라인 강의실과 PDF 생성 양쪽에서 서체가 깨지지 않아야 한다.

**본문 기본은 Medium(500)이다.** Regular가 아니다. 원본이 Medium을 압도적으로 많이 쓰며, 프로젝터에서 Regular는 얇아 보인다.

### 2.3 타이포 스케일

| 토큰 | px | weight | 용도 | 원본 근거 |
|---|---|---|---|---|
| `--fs-cover` | 40 | 700 | 표지 강의 제목 | 44pt × autofit 90% |
| `--fs-cover-label` | 24 | 700 | 표지 학기·과목 라벨 | 24pt |
| `--fs-title` | 28 | 700 | 슬라이드 제목 | 28pt, 레이아웃 고정값 |
| `--fs-lead` | 24 | 700 | 슬라이드 안 대제목 | 24pt (67회) |
| `--fs-sub` | 20 | 700 | 소제목 | 20pt (46회) |
| `--fs-emph` | 18 | 500 | 강조 본문, 표지 날짜 | 18pt (222회) |
| `--fs-body` | 14 | 500 | **본문 기본** | 14pt (668회, 최다) |
| `--fs-caption` | 12 | 400 | 그래프 축 라벨, 표 안 주석 | 11~12pt |
| `--fs-footer` | 10 | 400 | 푸터바 문구 | 10pt |
| `--fs-pagenum` | 11 | 400 | 페이지 번호 | 11pt |

행간은 본문 `1.55`, 제목 `1.2`다.

---

## 3. 색

### 3.1 토큰

```css
--brand-crimson: #892B3C;  /* 푸터바 배경 */
--define-crimson:#A82424;  /* 정의문·발문 본문 */
--accent-red:    #C00000;  /* 소제목·핵심 라벨 (최다 사용) */
--point-red:     #FF0000;  /* 그래프 좌표점, 결정적 강조 */
--accent-blue:   #0328C3;  /* 용어 부연, → 도출 결과 */
--chart-blue:    #4472C4;  /* 표 헤더, 예산선 */
--chart-orange:  #ED7D31;  /* 대비 곡선(이동 후) */
--accent-green:  #00B050;  /* 3번째 계열이 필요할 때만 */
--guide:         #B4C7E7;  /* 그래프 점선 보조선 */
--ink:           #1A1A1A;  /* 제목·본문 */
--ink-cover:     #262626;  /* 표지 제목 */
--muted:         #767676;  /* 표지 날짜, 비활성 (원본 #7F7F7F에서 대비 확보용 조정, §10) */
--rule:          #000000;  /* 표지 상하 구분선 */
--bg:            #FFFFFF;
```

### 3.2 역할 — 이 표 밖의 용도로 색을 쓰지 않는다

| 색 | 언제 쓰는가 | 실제 예 |
|---|---|---|
| `--accent-red` `#C00000` | 슬라이드 안 **소제목**과 핵심 용어 라벨 | 2강 p.5 "수요의 법칙", 3강 "예산제약 (budget constraint)" |
| `--define-crimson` `#A82424` | **정의문 한 문장**, 그리고 학생에게 던지는 **발문** | 2강 "Q) 원유가격이 폭등한다면 균형점은 어떻게 변할 것인가", "소비자가 교환으로부터 얻는 이득" |
| `--accent-blue` `#0328C3` | 표·그림의 **캡션**, `→`로 이어지는 **도출 결과** | 2강 p.2 "아이스크림의 가격에 따른 소비량", p.4 "→ 한계편익(marginal benefit)" |
| `--point-red` `#FF0000` | 그래프 위 **좌표점**과 화살표 | 모든 곡선 그래프의 빨간 점 |
| `--chart-blue` `#4472C4` | **표 헤더 배경**, 예산선 | 2강 수요표, 3강 예산선 |
| `--chart-orange` `#ED7D31` | 원래 곡선과 **대비되는 이동 후 곡선** | 2강 p.6 수요곡선 이동 |
| `--brand-crimson` `#892B3C` | **푸터바 배경 전용.** 텍스트 색으로 쓰지 않는다 | 전 슬라이드 하단 |

### 3.3 곡선의 색 — 도해 종류에 따라 다르다

기존 노트를 훑어보면 곡선 색에 두 가지 규칙이 섞여 있다. 어느 쪽인지 먼저 정하고 그린다.

| 도해 | 곡선 색 | 예 |
|---|---|---|
| **도출 과정** (표 → 곡선) | 검정 2.5px. 이동 후 곡선만 `--chart-orange` | 2강 p.2~6 수요곡선, p.11~12 공급곡선 |
| **균형 도해** (D와 S가 함께) | **D는 `--accent-blue`, S는 `--point-red`**, 3px | 2강 p.17~25 |
| **개별 주체 합산** (D₁·D₂·D₃ → Dₘ) | 개별은 초록·주황·파랑, 시장 합계는 검정 | 2강 p.7, p.13 |
| **정부 규제선** (상한·하한) | `#70AD47` 초록 1.6px 수평선 | 2강 p.20, p.21, p.25 |

CSS 클래스는 `.curve-demand`, `.curve-supply`, `.curve-alt`, `.curve-i1~i3`, `.curve-cap`이다.

**빨강 계열이 세 개인 이유**를 기억한다. `#C00000`은 라벨, `#A82424`는 문장, `#FF0000`은 그래프 점이다. 한 슬라이드에서 셋이 동시에 나오는 건 정상이지만, 서로 바꿔 쓰면 위계가 무너진다.

한 슬라이드에 **동시에 쓰는 색은 검정 포함 4종까지**로 제한한다.

---

## 4. 레이아웃

### 4.1 본문 슬라이드

```
┌──────────────────────────────────────────────────┐
│                                                  │
│  수요곡선의 도출            ← x=36, y=23, 28px/700 │
│                                                  │
│  ┌──────────────────────────────────────────┐   │
│  │ 콘텐츠 영역                                │   │
│  │ x=36 … 924 (폭 888), y=72 … 512           │   │
│  └──────────────────────────────────────────┘   │
│                                                  │
├──────────────────────────────────────────────────┤ ← y=524.4
│ Introduction to Urban Economics / …          26 │   h=15.6, #892B3C
└──────────────────────────────────────────────────┘
```

| 요소 | 값 | 근거 |
|---|---|---|
| 좌측 기준선 | **36px** | 제목 상자 x=25.8 + 내부 여백 7.2 |
| 우측 안전선 | 924px | 좌우 대칭 |
| 제목 baseline 영역 | y=23 ~ 58 | 레이아웃 title 상자 y=23.2 h=34.7 |
| 콘텐츠 시작 | y=72 | 콘텐츠 상자 y=71.6 |
| 콘텐츠 끝 | y=512 | 푸터바 위 12px 여유 |
| 푸터바 | y=524.4, h=15.6, 폭 전체 | 레이아웃 `직사각형 1` |
| 푸터 문구 | 좌 8px, 세로 중앙, 10px 흰색 | `Introduction to Urban Economics / Spring 2026 / KWON` |
| 페이지 번호 | 우 12px, 세로 중앙, 11px 흰색 | 레이아웃 sldNum 상자 |

푸터 문구는 `course.json`에서 읽어 자동으로 채운다. 슬라이드 HTML에 직접 쓰지 않는다.

### 4.2 표지 슬라이드

```
┌──────────────────────────────────────────────────┐
│                                          ⬤ 로고  │ x=848, y=11, 83×84
│  2026-1학기 도시경제학입문   ← 24px/700 #C00000   │ y=77
│ ──────────────────────────────────────────       │ y=122.9, 0.8px, x=0…927
│  2강. 수요와 공급, 그리고 시장의 균형  ← 40px/700   │ y=147
│ ──────────────────────────────────────────       │ y=222.9
│                                                  │
│                  2026. 3. 11.   ← 18px #767676   │ y=291, 가운데
│                                                  │
└──────────────────────────────────────────────────┘
```

표지에는 **푸터바가 없다.** 상하 구분선은 좌측 끝 `x=0`에서 시작해 `x=927.2`에서 끝난다 — 오른쪽이 열려 있는 것이 원본의 특징이므로 폭을 960으로 늘리지 않는다.

### 4.3 세로 밸런스 — 아래를 비워두지 않는다

콘텐츠가 위로 몰리고 아래 3분의 1이 비면 슬라이드가 허전해 보인다. 콘텐츠 영역(`y 72 ~ 512`, 높이 **440px**)을 고르게 쓴다.

- **2단 슬라이드**는 `.s-split .body`가 `align-items: center`다. 좌우 두 칸이 각자 세로 가운데로 놓여 남는 여백이 위아래로 나뉜다.
- **설명문 + 큰 그래프**가 세로로 쌓이는 슬라이드는 `<div class="body stack">`을 쓴다. 덩어리 전체가 세로 가운데로 간다.
- 누적 공개 요소는 숨겨져도 자리를 차지하므로, 가운데 정렬이어도 단계가 바뀔 때 흔들리지 않는다.

**크기 상한을 넘기지 않는다.** 가운데 정렬은 넘치는 콘텐츠를 위아래로 밀어내므로, 너무 크면 오히려 제목을 침범한다.

| 그래프 | 폭 | 근거 |
|---|---|---|
| 2단 우측 (`.fig-side`) | **500px** | 콘텐츠 888 − 좌단 355 − 간격 24 = 509 |
| 단독 (`.fig-solo`) | **570px** | 520×350 캔버스 → 384px 높이. 설명문 44 더해 428 ≤ 440 |
| 2분할 (`.fig-half`) | **430px** | 36 + 430 + 24 + 430 = 920 ≤ 우측 안전선 924 |

---

## 5. 슬라이드 유형 카탈로그

실물 예시는 `_templates/slide-patterns.html`에 있다. 새 슬라이드는 거기서 복사해 시작한다.

### T1. 표지 `.s-cover`
강의 첫 슬라이드. §4.2 그대로.

### T2. 발문 `.s-question`
도입부에서 학생에게 질문을 던지는 슬라이드. 큰 불릿 3개 안팎, `--define-crimson` 또는 검정 굵은 글씨. 답을 적지 않는다.
> 근거: 1강 p.2~3 "미국 금리인하와 엔비디아의 미래" / "강남 부동산은 불패인가?"

### T3. 정의 `.s-definition`
개념 하나를 정의한다. 제목 아래 `--define-crimson` 정의문 한 문장, 필요하면 아래쪽 화살표와 귀결.
> 근거: 1강 p.4 "경제학의 정의", 2강 p.22 "소비자잉여"

### T4. 개념 + 그래프 2단 `.s-split`
좌측 설명·표, 우측 그래프. **기존 노트에서 가장 많이 쓰인 유형이다.** 기본 분할은 좌 45% / 우 55%이며, 그래프가 주인공이면 40/60으로 넘긴다.
> 근거: 2강 p.2~6, 3강 p.7~8, p.10

### T5. 그래프 단독 + 주석 `.s-figure`
그래프를 크게 놓고 `--accent-red` 또는 `--accent-blue` 주석을 곁들인다.

### T6. 3분할 비교 `.s-triptych`
`(a) (b) (c)` 캡션(`--accent-red`, 12~14px, 가운데)을 단 그래프 3개를 나란히 놓는다. 각 셀 폭 약 280px.
> 근거: 3강 p.9 "특수한 경우의 무차별 곡선"

### T7. 수식 `.s-formula`
MathJax 블록 수식. 유도 과정은 한 줄에 하나씩, 결론 수식만 `--accent-red`.
> 근거: 3강 p.10 예산선 유도

### T8. 표 `.s-table`
`--chart-blue` 헤더에 흰 글씨, 본문 행은 `#D9E2F3` / `#E9EDF7` 교대. 셀 패딩 6px 12px, 14px Medium.
> 근거: 2강 p.2 아이스크림 수요표

### T9. 누적 공개 `.fragment`
아래 §8 참조. 별도 유형이 아니라 T3~T7에 얹는 속성이다.

### T10. 마무리 `.s-closing`
강의 마지막 슬라이드. `Q & A`(크림슨 34px) + 가로선 + 연락처. 푸터바를 넣지 않는다.
> 근거: 2강 p.26

### T11. 번호 목록 `.s-numbered`
"~의 결정요인" 같은 열거. 20px 굵은 `--accent-red` 번호 목록, 항목 간격 40px로 넉넉히.
> 근거: 2강 p.10 수요의 가격탄력성 결정요인, p.16 공급의 가격탄력성 결정요인

### 보조 요소: 우상단 노트 `.corner-note`
슬라이드 내내 참조해야 하는 짧은 규칙을 제목 오른쪽에 둔다.
> 근거: 2강 p.8, p.14 "탄력성 &gt; 1 → 탄력적 / 탄력성 &lt; 1 → 비탄력적"

---

## 6. 그래프

### 6.1 개념도는 인라인 SVG로 그린다

기존 노트의 곡선은 전부 실측 데이터가 아니라 **손으로 그린 개념도**다. 차트 라이브러리로 흉내 내면 축·격자·범례가 딸려 나와 원본과 달라진다. 그래서 개념도는 SVG로 직접 그린다.

`_shared/js/charts.js`에 재사용 함수가 있다:

| 함수 | 그리는 것 |
|---|---|
| `axes(opts)` | 축 두 개 + 축 라벨 |
| `curve(points, opts)` | 부드러운 곡선 (수요·공급·무차별) |
| `line(p1, p2, opts)` | 직선 (예산선, 완전대체재) |
| `guide(x, y, opts)` | 점선 보조선 + 축 눈금값 |
| `dot(x, y, label)` | 빨간 좌표점 + 라벨 |
| `annot(x, y, text, opts)` | 주석 텍스트 |
| `arrow(p1, p2, opts)` | 화살표 |

### 6.2 SVG 규격

| 항목 | 값 |
|---|---|
| 기본 캔버스 | `viewBox="0 0 520 320"` — 플롯 영역은 x 55…410, y 25…255 |
| 축 | `#000` 1px, 화살촉 없음, 원점에서 위·오른쪽으로만 |
| 곡선 | `#000` **2.5px**, `fill:none`, `stroke-linecap:round` |
| 대비 곡선 | `--chart-orange` 2.5px |
| 예산선 | `--chart-blue` 2.5px |
| 점선 보조선 | `--guide` 1px, `stroke-dasharray="3 3"` |
| 좌표점 | `--point-red` 채움, `r=4.5` |
| 축 이름 | 12px **700**, 축 끝 바깥 |
| 눈금값 | 12px 400, 축 바깥 6px |
| 주석 | 12~14px, `--accent-red` 또는 `--accent-blue` |

축 눈금은 **설명에 등장하는 값만** 찍는다. 2강 수요곡선은 눈금이 `25, 20, 5` / `8, 12, 29` 뿐이다.

**주의할 점 두 가지.**

1. **캔버스 오른쪽·아래에 여유를 둔다.** x축 이름은 축 끝 바깥(x ≈ 424)에 놓이므로 viewBox 폭이 440이면 잘린다. 520을 쓴다. 축 왼쪽에 라벨(예: "균형가격")을 붙일 때는 `viewBox="-62 0 582 320"`처럼 원점을 음수로 밀어 자리를 만든다.
2. **삼각형·사각형 채움은 곡선 위의 점을 써야 한다.** 잉여 영역을 그릴 때 꼭짓점이 곡선에서 벗어나면 색칠이 선 밖으로 삐져나온다. 직선 `(x₁,y₁)–(x₂,y₂)` 위의 점은 손으로 계산해 주석으로 남긴다.

MathJax는 SVG 안의 `<text>`를 처리하지 않는다. 그래프 안의 수식 기호는 LaTeX 대신 유니코드로 쓴다 (`P̄`, `Q̄`, `ε`, `Δ`).

### 6.3 Chart.js는 실제 데이터일 때만

통계·실증 데이터를 보일 때만 `_shared/vendor/chartjs/chart.umd.min.js`를 쓴다. 개념도에는 쓰지 않는다.

```js
// PDF 캡처가 애니메이션 도중에 찍히면 안 된다
options: {
  animation: false,
  responsive: false,       // 960×540 고정 좌표계이므로 반응형 불필요
  plugins: { legend: { display: false } },
  scales: { x: { grid: { display: false } }, y: { grid: { color: '#EEE' } } }
}
```

색은 반드시 §3 토큰에서 가져온다. Chart.js 기본 팔레트를 쓰지 않는다.

### 6.4 좌표점은 손으로 찍지 않는다 — 곡선에서 구한다

**이 프로젝트에서 가장 자주 났던 오류다.** 곡선은 함수가 그리는데 점은 사람이 눈대중으로 찍으면, 점이 곡선에서 벗어난다. 2강 초안에서 최대 **37px**까지 어긋나 있었다.

**표가 있는 그래프는 `Fig.scale()`을 쓴다.** 곡선·눈금·점을 전부 같은 수에서 뽑으면 어긋날 수가 없다.

```js
const S = Fig.scale({ x: [0, 34], y: [0, 29] });     // 데이터 범위 → 픽셀
const DATA = [[8, 25], [12, 20], [16, 15], [22, 10], [29, 5]];   // 표 그대로

Fig.curve(S.curve(DATA)),                             // 곡선
Fig.guide(...S.p([12, 20]), { xtick: '12', ytick: '20' }),   // 보조선·눈금
Fig.dot(...S.p([12, 20])),                            // 점 — 반드시 곡선 위
```

곡선을 **이동**할 때도 픽셀이 아니라 데이터에서 옮긴다. 그래야 새 곡선 위의 점이 정확히 맞는다.

```js
const SHIFTED = DATA.map(([q, p]) => [q + 6, p]);     // 오른쪽으로 6단위
```

**데이터 점이 아닌 자리**(가격상한선이 곡선과 만나는 곳 등)는 `Fig.at()`으로 곡선에서 계산한다.

```js
const onS = Fig.at(supplyPts, { y: yCap });   // 상한선이 공급곡선과 만나는 점
const dTop = Fig.at(demandPts, { x: onS[0] });
```

**곡선은 단조 보간(PCHIP)으로 그린다.** `Fig.curve()`가 알아서 한다.

점을 지나게 하는 것과 곡선이 매끄러운 것은 상충하지 않는다. 처음에 쓰던 Catmull-Rom 보간은 점 간격이 고르지 않으면(8→12→16→22→29) 구간마다 장력이 달라져 곡선이 출렁이고 꺾였다. **Fritsch–Carlson 단조 보간**은 기울기를 조화평균으로 잡아 넘침과 역주행을 원천적으로 막으므로, 모든 데이터 점을 정확히 지나면서도 매끈하다. 수요·공급곡선처럼 한 방향으로만 가는 데이터에 맞는 방식이다.

`Fig.curve()`는 x가 증가하는 점묶음이면 자동으로 단조 보간을, 아니면 Catmull-Rom을 쓴다. 따로 지정할 것은 없다.

> 곡선이 여전히 각져 보이면 보간이 아니라 **표 자체**를 의심한다. 2강 수요표는 (8,25)→(12,20)→(16,15) 구간의 기울기가 완전히 같아 그 부분이 원래 직선이다.

### 표가 없는 순수 개념도라면

수치 근거가 없는 그림은 매끄러운 형태를 먼저 그리고 **점을 곡선 위로 스냅**시킨다.

```js
const C = Fig.convex({ x0: 76, x1: 258, yTop: 30, yBot: 162 });
Fig.dot(x, null, { on: C })      // 곡선 위로 붙는다
```

**자동 검사.** `Fig.draw()`는 그릴 때마다 각 점에서 가장 가까운 곡선까지의 거리를 재서 2.5px을 넘으면 콘솔에 경고한다. `_scripts/check-figures.sh`가 그 경고를 모아 보여준다. **PDF를 뽑기 전에 반드시 돌린다.**

### 6.5 겹침 — 먼저 피하고, 마지막에 가린다

순서대로 적용한다.

1. **자리를 옮겨 피한다.** 곡선 아래 여백이 없으면 주석을 **축 아래**로 내린다 (`Fig.BOX.y0 + 40`, 눈금값보다 아래). 캔버스 높이도 그만큼 키운다.
2. **곡선을 피해 배치한다.** 라벨을 붙일 자리는 곡선의 반대쪽을 고른다 — 우하향 곡선이면 오른쪽 위가 비어 있다.
3. **그래도 겹치면 흰 테두리로 읽히게 한다.** 그래프 안의 모든 `<text>`에 `paint-order: stroke; stroke: #FFF; stroke-width: 3px`가 이미 걸려 있다. 개념도에서는 곡선에 붙여야 뜻이 통하는 라벨이 있으므로 마지막 안전장치로 둔다.

**캔버스를 넘지 않게 한다.** 가로축 이름은 축 끝 바깥(`BOX.x1 + 14`)에 놓이므로, 한글 이름 길이(약 90~100px)만큼 viewBox에 여유가 있어야 한다. 없으면 `BOX.x1`을 줄인다. `check-figures.sh`가 잘린 글자를 잡아낸다.

### 6.6 채움 영역 안의 라벨은 `Fig.labelIn()`으로 넣는다

"소비자잉여" 같은 라벨을 좌표로 찍으면 두 줄짜리 글자 상자의 모서리가 삼각형 빗변을 넘어 밖으로 삐져나온다. 눈으로는 잘 안 보이지만 인쇄하면 드러난다.

```js
F.labelIn([[F.BOX.x0, S.y(1800)], [F.BOX.x0, yP], [B[0], yP]],
          '소비자\n잉여', { tone: 'red', size: 15 })
```

다각형을 주면 **무게중심에 놓고, 그려진 뒤 실제 글자 크기를 재서 안쪽으로 밀어넣는다.** 그래도 안 들어가면 글자를 1px씩 줄여(최소 10px) 다시 시도한다. 영역이 라벨보다 작아 끝내 못 넣으면 콘솔에 알린다 — 그때는 영역을 넓히거나 라벨을 밖으로 빼고 지시선을 단다.

`check-figures.sh`가 글자 상자의 네 모서리를 모두 검사하므로, 삐져나온 채로 배포될 일이 없다.

**영역을 의미대로 나눈다.** 순사회 편익처럼 한 덩어리를 둘로 나눠 부를 때는, 가격선을 경계로 위·아래 다각형을 각각 넘긴다. 그래야 라벨이 자기 영역 안에 머문다.

### 6.7 막대와 곡선은 `Fig.bars()`로 함께 만든다

막대그래프 위를 지나는 곡선은 **각 막대의 가로 중앙에서 막대 꼭대기를 지나야** 한다. 직선을 따로 눈대중으로 그으면 어긋난다 — 2강 초안에서 오른쪽 끝이 막대 위로 **32px** 떠 있었다.

```js
const B1 = F.bars(S, [180, 300, 420, 550, 670, 800]);
F.raw(B1.svg)        // 막대
...B1.labels         // 1 2 3 … 축 라벨
```

`B1.mid`가 각 막대 꼭대기의 중앙점(데이터 좌표)이다. 직선을 그을 때는 **첫 막대와 끝 막대의 중앙점을 정확히 지나도록** 두 끝점을 계산해 쓴다.

```js
// (0.5,180)과 (5.5,800)을 지나는 직선 → 기울기 124/단위
const S1 = S.curve([[0, 118], [10.2, 1383]]);
```

---

## 7. 수식

MathJax 4 `tex-svg` (`_shared/vendor/mathjax-tex-svg.js`)를 로컬에서 로드한다. SVG 출력이므로 PDF에서 벡터로 남는다.

- 인라인 `$...$`, 블록 `$$...$$`
- 변수는 이탤릭(TeX 기본), 함수명·단위는 `\mathrm{}`
- 블록 수식 앞뒤 여백 12px

**수식에 색을 줄 때 `\color{}`를 쓰지 않는다.** 그 명령은 MathJax가 `input/tex/extensions/color.js`를 따로 내려받게 만드는데, 우리는 단일 번들만 vendor하므로 파일이 없어 수식이 통째로 렌더되지 않는다. 대신 **바깥 요소에 CSS 색을 준다** — MathJax의 SVG 출력은 `currentColor`를 따르므로 그대로 물든다. 토큰을 쓸 수 있다는 이점도 있다.

```html
<!-- 안 됨: 확장 파일이 없어 렌더 실패 -->
<p>$$\color{#C00000}{Y = -\frac{P_x}{P_y}X + \frac{M}{P_y}}$$</p>

<!-- 맞음 -->
<p class="formula-key">$$Y = -\frac{P_x}{P_y}X + \frac{M}{P_y}$$</p>
```

---

## 8. 누적 공개

기존 PPT는 **같은 슬라이드를 여러 장 복제해** 요소를 하나씩 늘렸다. 2강 p.2→3→4→5가 한 슬라이드의 4단계이고, 3강 p.7→8도 마찬가지다.

HTML에서는 **한 슬라이드 + `.fragment`** 로 대체한다.

```html
<section class="s-split">
  <h2>수요곡선의 도출</h2>
  <div class="col-l">
    <p class="caption-blue">아이스크림의 가격에 따른 소비량</p>
    <table>…</table>
  </div>
  <div class="col-r">
    <svg class="fragment" data-fragment-index="0">…곡선…</svg>
    <p class="annot fragment" data-fragment-index="1">→ 한계편익(marginal benefit)</p>
  </div>
</section>
```

규칙:
- `fade-in`만 쓴다. `fade-up`, `zoom-in` 등 이동을 동반하는 효과는 쓰지 않는다 — 레이아웃이 흔들려 판서 중 시선이 끊긴다.
- 한 슬라이드의 fragment는 **4단계까지**. 넘으면 슬라이드를 나눈다.
- PDF 빌드 시 fragment마다 페이지가 분리되므로(`pdfSeparateFragments: true`), 결과 PDF의 장수는 기존 PDF와 같아진다.

---

## 9. 인쇄·PDF

`_shared/theme/print.css`가 다음을 보정한다.

- 배경색·푸터바를 강제 인쇄 (`print-color-adjust: exact`)
- fragment를 전부 보이게 (Reveal의 print-pdf 모드가 처리하나, 명시적으로 한 번 더)
- 페이지 여백 0, 페이지 크기 슬라이드와 동일

PDF는 `_scripts/build-pdf.sh`로 만든다. 브라우저 인쇄 대화상자에 의존하지 않는다 — 사용자 브라우저마다 결과가 달라지기 때문이다.

---

## 10. 접근성

흰 배경 기준 실측 대비비다.

| 색 | 대비비 | 판정 |
|---|---|---|
| `--ink` `#1A1A1A` | 17.4:1 | 본문 가능 |
| `--ink-cover` `#262626` | 15.1:1 | 본문 가능 |
| `--accent-blue` `#0328C3` | 10.0:1 | 본문 가능 |
| `--brand-crimson` `#892B3C` | 8.5:1 | 본문 가능 |
| `--define-crimson` `#A82424` | 7.1:1 | 본문 가능 |
| `--accent-red` `#C00000` | 6.5:1 | 본문 가능 |
| `--chart-blue` `#4472C4` | 4.7:1 | 본문 가능 (경계) |
| `--muted` `#767676` | 4.5:1 | 본문 가능 (경계) |
| `--point-red` `#FF0000` | **4.0:1** | 텍스트 불가 |
| `--accent-green` `#00B050` | **2.9:1** | 텍스트 불가 |
| `--chart-orange` `#ED7D31` | **2.8:1** | 텍스트 불가 |
| `--guide` `#B4C7E7` | 1.7:1 | 보조선 전용 |

- `--muted`는 원본의 `#7F7F7F`(4.0:1, 미달)를 **`#767676`(4.5:1)로 한 단계만 조정한 값**이다. 육안 차이는 없고 표지 날짜가 기준을 통과한다.
- **`--point-red`, `--chart-orange`, `--accent-green`은 도형 전용이다.** 곡선·점·화살표에만 쓰고, 읽어야 할 문장에는 쓰지 않는다. 문장 강조는 `--define-crimson`이나 `--accent-red`로 한다.
- 흰 글씨를 얹는 배경: `--brand-crimson` 8.5:1, `--chart-blue` 4.7:1 — 둘 다 통과한다.
- 색만으로 구분하지 않는다. 곡선 두 개를 비교할 때는 색과 함께 라벨(`D`, `D'`)을 붙인다. 2강 p.6이 검정/주황 곡선에 `A → B`, `A → A'` 라벨을 단 것이 그 예다.
- 그래프 `<svg>`에 `role="img"`와 `<title>`을 넣어 무엇을 그린 것인지 적는다.

---

## 11. 하지 않는 것

- 그라데이션 배경, 그림자, 둥근 모서리 카드 — 원본에 없다
- 이모지 불릿, 아이콘 폰트
- 슬라이드 전환 효과 (`transition: none`)
- 한 슬라이드 8줄 초과
- 색 4종 초과 동시 사용
- 외부 CDN 링크 (서체·스크립트·이미지 전부 로컬)
- `%`·`vw`·`rem` 기반 크기 지정 (960×540 px 좌표계를 쓴다)
- 표에 세로 괘선 — 원본은 가로 줄무늬만 쓴다
