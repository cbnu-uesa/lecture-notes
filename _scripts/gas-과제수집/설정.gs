/**
 * 설정 — 여기만 고친다.
 *
 * 실제 ID 는 저장소에 올리지 않는다. 채운 값은
 * 2026-2학기_데이터사이언스/private/과제-수집-설정.md 에 적어 두고
 * Apps Script 편집기에서 이 파일에 옮긴다.
 */

/** 과제 루트 폴더 ID. 드라이브에서 폴더를 열었을 때 주소 끝의 긴 문자열. */
const ROOT_FOLDER_ID = '여기에-루트-폴더-ID';

/**
 * 과제 제출 폼 ID. **한 학기에 하나뿐이다.**
 * 편집 주소 docs.google.com/forms/d/<여기>/edit 의 가운데 토막.
 * 응답 주소(/e/ 로 시작하는 것)가 아니다.
 */
const 폼_ID = '여기에-폼-ID';

/**
 * 주차 드롭다운의 항목이자 폴더 이름이다. 학생이 고른 값이 곧 폴더가 된다.
 * 목록에 없는 값이 들어오면 폴더를 만들지 않고 오류 메일을 보낸다 —
 * 오타 하나로 엉뚱한 폴더가 생기는 것을 막는다.
 *
 * 주차를 더하거나 이름을 고친 뒤에는 주차항목갱신() 을 돌려 폼에 반영한다.
 */
const 주차목록 = [
  '02주차_클로드코드와위키',
  '03주차_SGIS스킬',
  '04주차_연구질문',
  '05주차_데이터수집과깃',
  '06주차_기술적분석1',
  '07주차_기술적분석2',
  '09주차_진단적분석1',
  '10주차_진단적분석2',
  '11주차_예측적분석1',
  '12주차_예측적분석2',
  '13주차_처방적분석',
  '14주차_개인프로젝트',
];

/** 질문 제목. 스크립트가 이 이름으로 찾으므로 폼에서 바꾸지 않는다. */
const Q = {
  주차: '주차',
  학번: '학번',
  이름: '이름',
};

/** 학번 자릿수. 폼의 응답 검증과 점검() 의 오타 표시에 함께 쓴다. */
const 학번자릿수 = 10;

/** 시간대. 파일 이름의 제출 시각을 이 기준으로 찍는다. */
const TZ = 'Asia/Seoul';

/**
 * 오류 알림을 받을 주소. 비워 두면 스크립트를 승인한 계정으로 보낸다.
 * 알림을 끄지 않는다 — 트리거가 죽으면 이것 말고는 알 길이 없다.
 */
const 알림받을주소 = '';
